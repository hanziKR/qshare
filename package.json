{
  "name": "qshare",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc --build",
    "start": "npm run build && webpack && electron .",
    "packrun": "webpack && electron .",
    "watch": "tsc-watch --silent --onSuccess \"npm run packrun\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "electron": "^20.1.0",
    "tsc-watch": "^5.0.3",
    "typescript": "^4.8.2",
    "webpack": "^5.74.0"
  },
  "devDependencies": {
    "webpack-cli": "^4.10.0"
  }
}